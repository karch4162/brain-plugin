# log.md — append-only operation log

One line per session/operation that changed the vault. Appended by `/brain:save` and `bin/sync-graph.sh`.

- {{DATE}} — vault initialized from the brain plugin skeleton.
